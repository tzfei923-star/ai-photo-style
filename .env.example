# 'local'（免費，用自己電腦的 Stable Diffusion）或 'openai'（付費 API）
GENERATION_BACKEND=local

# GENERATION_BACKEND=local 時使用，需先啟動 stable-diffusion-webui（含 --api）
SD_WEBUI_URL=http://127.0.0.1:7860

# GENERATION_BACKEND=openai 時才需要
OPENAI_API_KEY=sk-your-key-here

PORT=3000
